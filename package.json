{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start-fe": "lerna run start --scope=client",
    "storybook": "lerna run storybook --scope=client"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  }
}
